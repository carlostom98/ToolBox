package com.poc.postitapp.domain.usecases.persistancecases

import com.poc.postitapp.data.persistance.IRoomPersistenceRepository

class AddPostItUseCase(private val repository: IRoomPersistenceRepository) {
}